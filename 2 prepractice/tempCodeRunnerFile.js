const Name = N_CT.filter((people) => {
//     // Filter people based on the total_completed_task value
//     return people.user.total_completed_task === 2; // Correct access to 'total_completed_task'
// }).map(people => people.user.name); // Extract just the names of filtered people

// console.log(Name);